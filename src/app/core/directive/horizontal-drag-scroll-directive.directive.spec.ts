import { HorizontalDragScrollDirective } from './horizontal-drag-scroll-directive.directive';

describe('HorizontalDragScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new HorizontalDragScrollDirective();
    expect(directive).toBeTruthy();
  });
});
