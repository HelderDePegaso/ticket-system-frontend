import { DateOrDashPipe } from './date-or-dash.pipe';

describe('DateOrDashPipe', () => {
  it('create an instance', () => {
    const pipe = new DateOrDashPipe();
    expect(pipe).toBeTruthy();
  });
});
